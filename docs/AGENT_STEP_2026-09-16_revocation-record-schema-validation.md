# Revocation record schema validation

Temporary validation branch marker. The branch contains the durable revocation
schema hardening and regression tests from `main` so the normal pull-request
CI matrix can validate the exact implementation before the branch is closed.
